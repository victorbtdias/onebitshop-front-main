import React from "react";
import { Container, ErrorTitle } from "./styled";

const ErrorScreen = () => {
  return (
    <Container>
      <ErrorTitle>Está tela teve algum erro</ErrorTitle>
    </Container>
  );
};

export default ErrorScreen;
